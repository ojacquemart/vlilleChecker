buildscript {
    repositories {
        mavenCentral()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:0.9.+'
    }
}

allprojects {
    group = 'com.vlille.checker'
    version = '3.0.2-SNAPSHOT'

    repositories {
        mavenCentral()
    }
}